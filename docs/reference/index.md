# 参考索引

> ⚠️ **生成区**：此页内容本应由 `docsforge scan` 自动生成，请勿手工编辑。
> 当前工具能力未落地（多文件扫描 / 模块索引尚在 roadmap S3/L1），
> 以下是**目标形态占位**，展示"模块/类/函数索引"该长什么样。

<!-- BEGIN GENERATED: module-index -->

## 模块地图

| 模块 | 说明 | 主要导出 |
|---|---|---|
| `docsforge.tags` | `docs:` 标签解析器（语言无关） | `parse_docs_tags` |
| `docsforge.model` | IR：`TaggedDeclaration` | `TaggedDeclaration` |
| `docsforge.registry` | Handler 协议 + 注册表（插件点） | `register` / `get_handler` |
| `docsforge.render` | Markdown 渲染器（语言无关） | `render_api_grouped_table` |
| `docsforge.drift` | 生成区替换 + 漂移检查 | `check_drift` / `replace_or_insert` |
| `docsforge.cli` | 命令行入口 | `main` |

<!-- END GENERATED: module-index -->
